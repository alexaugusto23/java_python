def conv_real_dolar(valor, txreal):
    resultado = float(valor) * float(txreal)
    return resultado

def conv_dolar_real(valor, txdolar):
    resultado = float(valor) * float(txdolar)
    return resultado 
